// Ship browser automation with OpenWork instead of asking OpenCode to download
// the package while a person's first workspace is booting. This wrapper is
// bundled into the same runtime plugin directory as OpenWork's own plugins.
export { default, server } from "opencode-chrome-devtools";
