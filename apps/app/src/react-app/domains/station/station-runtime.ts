/**
 * Compatibility seam for the app adapter. The Station lifecycle and
 * provenance contracts live in @openwork/station so native, web, and future
 * integrations do not depend on the React domain.
 */
export * from "@openwork/station";
