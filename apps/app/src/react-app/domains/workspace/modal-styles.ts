// Workspace modal style tokens live in src/app/workspace/modal-styles.ts
// (framework-agnostic Tailwind class strings, also consumed by Solid). This
// module re-exports them so the React tree imports from a `react-app` path
// that matches the target domain layout.
export * from "../../../app/workspace/modal-styles";
