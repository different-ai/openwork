extends: ./electron-builder.base.yml
appId: com.differentai.micx
productName: Micx
protocols:
  - name: Micx
    schemes:
      - micx
linux:
  executableName: micx
artifactName: micx-${os}-${arch}-${version}.${ext}
