extends: ./electron-builder.base.yml
appId: com.differentai.micx
productName: Micx Cloud
extraMetadata:
  micxDistribution: cloud
protocols:
  - name: Micx Cloud
    schemes:
      - micx
publish:
  - provider: github
    owner: different-ai
    repo: micx
    releaseType: release
    channel: cloud
linux:
  executableName: micx-cloud
artifactName: micx-cloud-${os}-${arch}-${version}.${ext}
