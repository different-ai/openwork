extends: ./electron-builder.base.yml
appId: com.differentai.micx
productName: Micx Enterprise
extraMetadata:
  micxDistribution: enterprise
protocols:
  - name: Micx Enterprise
    schemes:
      - micx
publish:
  - provider: github
    owner: different-ai
    repo: micx
    releaseType: release
    channel: enterprise
linux:
  executableName: micx-enterprise
artifactName: micx-enterprise-${os}-${arch}-${version}.${ext}
