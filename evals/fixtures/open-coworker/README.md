# Open Coworker journey fixtures

Files a journey points the app at through its environment so the local mode
screen has something to find. Every value is plainly fake and never a working
credential.

- `codex-home/auth.json` — the shape Codex keeps a ChatGPT sign-in in; used as
  `CODEX_HOME` by `open-coworker-local-first`.
