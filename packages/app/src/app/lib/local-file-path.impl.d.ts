/**
 * Normalizes a local file path from various formats (file:// URIs, plain paths)
 * @param value - The path string to normalize
 * @returns The normalized file path
 */
export declare function normalizeLocalFilePath(value: string): string;
