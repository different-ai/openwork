services:
  mysql:
    image: mysql:8.4
    container_name: micx-web-local-mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: micx_den
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -ppassword --silent"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 10s
    volumes:
      - micx-web-local-mysql-data:/var/lib/mysql

volumes:
  micx-web-local-mysql-data:
