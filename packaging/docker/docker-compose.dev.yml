# docker-compose.dev.yml — Dev testability stack (no custom Dockerfile)
#
# Usage (from repo root):
#   docker compose -f packaging/docker/docker-compose.dev.yml up
#
# Then open the printed Web UI URL — already wired to headless, no config needed.
#
# Env overrides (optional, via .env or export):
#   MICX_TOKEN        — shared client token (auto-generated if unset)
#   MICX_HOST_TOKEN   — host/admin token (auto-generated if unset)
#   MICX_WORKSPACE    — host path to mount as workspace (default: ./workspace)
#   MICX_PORT         — host port to map to container :8787 (default: 8787)
#   WEB_PORT              — host port to map to container :5173 (default: 5173)
#   MICX_DEV_ID       — unique ID for this stack (default: default)
#   MICX_DEV_MODE     — enables isolated OpenCode dev state
#   MICX_PUBLIC_HOST  — browser-facing host/IP for LAN-accessible URLs
#   MICX_DOCKER_DEV_MOUNT_HOST_OPENCODE=1 — import host OpenCode config/auth into the isolated dev state
#   MICX_OPENCODE_CONFIG_DIR      — host OpenCode config dir detection override
#   MICX_OPENCODE_DATA_DIR        — host OpenCode data dir detection override
#   MICX_HOST_OPENCODE_CONFIG_DIR — import source for OpenCode config
#   MICX_HOST_OPENCODE_DATA_DIR   — import source for OpenCode auth/data

x-shared: &shared
  image: node:22-bookworm-slim
  working_dir: /app
  volumes:
    # Mount the entire repo so both services share node_modules + source
    - ../../:/app
    - pnpm-store:/root/.local/share/pnpm/store
    - bun-install:/root/.bun
    - ${MICX_WORKSPACE:-./workspace}:/workspace
    - ${MICX_HOST_OPENCODE_CONFIG_DIR:-./workspace/.micx-host-opencode-config}:/persist/.config/opencode:ro
    - ${MICX_HOST_OPENCODE_DATA_DIR:-./workspace/.micx-host-opencode-data}:/persist/.micx-host-opencode-data:ro

services:
  micx-server:
    <<: *shared
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -e

        # --- Ensure workspace dir is writable ---
        mkdir -p /workspace && touch /workspace/.keep 2>/dev/null || true

        # --- Install system deps ---
        apt-get update -qq && apt-get install -y -qq --no-install-recommends \
          curl ca-certificates unzip git >/dev/null 2>&1

        # --- Install bun (cached in named volume) ---
        export BUN_INSTALL="/root/.bun"
        export PATH="$$BUN_INSTALL/bin:$$PATH"
        if ! command -v bun >/dev/null 2>&1; then
          echo "[micx-server] Installing bun..."
          curl -fsSL https://bun.sh/install | bash
        fi

        # --- Enable pnpm via corepack ---
        corepack enable && corepack prepare pnpm@10.27.0 --activate

        # --- Install deps ---
        echo "[micx-server] Installing dependencies..."
        pnpm install --no-frozen-lockfile --network-concurrency 1 --child-concurrency 1

        # --- Build the server plugins and install the pinned OpenCode binary ---
        pnpm --filter micx-server build
        OPENCODE_VERSION=$$(node -e 'const fs=require("fs"); const parsed=JSON.parse(fs.readFileSync("/app/constants.json", "utf8")); process.stdout.write(String(parsed.opencodeVersion || "").trim().replace(/^v/, ""));')
        export OPENCODE_INSTALL_DIR="/root/.opencode/bin"
        mkdir -p "$$OPENCODE_INSTALL_DIR"
        if ! command -v opencode >/dev/null 2>&1; then
          curl -fsSL https://opencode.ai/install | bash -s -- --version "$$OPENCODE_VERSION" --no-modify-path
        fi
        export PATH="$$OPENCODE_INSTALL_DIR:$$PATH"
        export MICX_MANAGE_OPENCODE=1
        export MICX_OPENCODE_BIN="$$(command -v opencode)"
        export MICX_EXTENSIONS_PLUGIN_DIR="/app/apps/server/dist/opencode-plugins"

        # --- Resolve tokens ---
        if [ -z "$$MICX_TOKEN" ]; then
          MICX_TOKEN=$$(cat /proc/sys/kernel/random/uuid)
          export MICX_TOKEN
        fi
        if [ -z "$$MICX_HOST_TOKEN" ]; then
          MICX_HOST_TOKEN=$$(cat /proc/sys/kernel/random/uuid)
          export MICX_HOST_TOKEN
        fi

        # Write tokens so the web service can source them
        mkdir -p /app/tmp
        printf 'MICX_TOKEN=%s\nMICX_HOST_TOKEN=%s\n' \
          "$$MICX_TOKEN" "$$MICX_HOST_TOKEN" > "/app/tmp/.dev-env-${MICX_DEV_ID:-default}"

        echo ""
        echo "============================================"
        echo "  Micx server"
        echo "  Server:     http://localhost:${MICX_PORT:-8787}"
        echo "  Health:     http://localhost:${MICX_PORT:-8787}/health"
        echo "  Token:      $$MICX_TOKEN"
        echo "  Host token: $$MICX_HOST_TOKEN"
        echo "============================================"
        echo ""

        exec pnpm --filter micx-server exec bun dist/cli.js \
          --workspace /workspace \
          --host 0.0.0.0 \
          --port 8787 \
          --token "$$MICX_TOKEN" \
          --host-token "$$MICX_HOST_TOKEN" \
          --approval auto \
          --cors "*" \
          --verbose
    ports:
      - "${MICX_PORT:-8787}:8787"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8787/health || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 90s
    environment:
      CI: "true"
      MICX_TOKEN: ${MICX_TOKEN:-}
      MICX_HOST_TOKEN: ${MICX_HOST_TOKEN:-}
      MICX_DEV_ID: ${MICX_DEV_ID:-default}
      MICX_DEV_MODE: ${MICX_DEV_MODE:-1}
      MICX_DEV_OPENCODE_IMPORT_CONFIG_DIR: /persist/.config/opencode
      MICX_DEV_OPENCODE_IMPORT_DATA_DIR: /persist/.micx-host-opencode-data

  web:
    <<: *shared
    depends_on:
      micx-server:
        condition: service_healthy
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -e

        # --- Install system deps ---
        apt-get update -qq && apt-get install -y -qq --no-install-recommends \
          curl ca-certificates >/dev/null 2>&1

        # --- Bun + pnpm ---
        export BUN_INSTALL="/root/.bun"
        export PATH="$$BUN_INSTALL/bin:$$PATH"
        if ! command -v bun >/dev/null 2>&1; then
          curl -fsSL https://bun.sh/install | bash
        fi
        corepack enable && corepack prepare pnpm@10.27.0 --activate

        # --- Read token written by micx-server ---
        if [ -f "/app/tmp/.dev-env-${MICX_DEV_ID:-default}" ]; then
          . "/app/tmp/.dev-env-${MICX_DEV_ID:-default}"
          export VITE_MICX_TOKEN="$$MICX_TOKEN"
        fi

        echo ""
        echo "============================================"
        echo "  Micx web UI"
        echo "  URL:   http://localhost:${WEB_PORT:-5173}"
          echo "  Token: $${VITE_MICX_TOKEN:-<see micx-server logs>}"
        echo "============================================"
        echo ""

        export VITE_MICX_URL="http://${MICX_PUBLIC_HOST:-localhost}:${MICX_PORT:-8787}"
        export VITE_MICX_PORT="${MICX_PORT:-8787}"
        export VITE_ALLOWED_HOSTS="all"
        export HOST="0.0.0.0"
        export PORT="5173"

        exec pnpm --filter @micx/app exec vite \
          --host 0.0.0.0 \
          --port 5173 \
          --strictPort
    ports:
      - "${WEB_PORT:-5173}:5173"
    environment:
      MICX_DEV_ID: ${MICX_DEV_ID:-default}

volumes:
  pnpm-store:
    name: micx-dev-pnpm-store
  bun-install:
    name: micx-dev-bun-install
