{{- if .Values.migrations.enabled }}
{{- $migrationMode := default "apply" .Values.migrations.mode }}
{{- if not (has $migrationMode (list "apply" "check")) }}
{{- fail "migrations.mode must be either apply or check" }}
{{- end }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "openwork-ee.fullname" . }}-migrate
  labels:
    {{- include "openwork-ee.componentLabels" (dict "root" . "component" "migration") | nindent 4 }}
  {{- if .Values.migrations.hook }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-5"
    {{- with .Values.migrations.hookDeletePolicy }}
    "helm.sh/hook-delete-policy": {{ . | quote }}
    {{- end }}
  {{- end }}
spec:
  backoffLimit: {{ .Values.migrations.backoffLimit }}
  activeDeadlineSeconds: {{ .Values.migrations.activeDeadlineSeconds }}
  template:
    metadata:
      labels:
        {{- include "openwork-ee.componentSelectorLabels" (dict "root" . "component" "migration") | nindent 8 }}
      {{- with .Values.migrations.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      restartPolicy: Never
      terminationGracePeriodSeconds: 30
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: migrate
          image: "{{ .Values.denApi.image.repository }}:{{ default .Values.image.tag .Values.denApi.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            {{- toYaml .Values.migrations.command | nindent 12 }}
          args:
            {{- toYaml .Values.migrations.args | nindent 12 }}
            {{- if eq $migrationMode "check" }}
            - --check
            {{- end }}
          env:
            - name: DB_MODE
              value: {{ .Values.config.databaseMode | quote }}
            - name: DATABASE_URL
              {{- if .Values.secret.create }}
              value: {{ .Values.secret.values.databaseUrl | quote }}
              {{- else }}
              valueFrom:
                secretKeyRef:
                  name: {{ include "openwork-ee.secretName" . }}
                  key: {{ .Values.secret.keys.databaseUrl }}
              {{- end }}
            - name: MIGRATION_LOCK_TIMEOUT_SECONDS
              value: {{ .Values.migrations.lockTimeoutSeconds | quote }}
            - name: MIGRATION_LOCK_LEASE_SECONDS
              value: {{ .Values.migrations.lockLeaseSeconds | quote }}
          {{- with .Values.migrations.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
{{- end }}
